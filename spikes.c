#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int getTwoDigit(char* expression){
	char* digit = calloc(3,sizeof(int));
	for (int i = 0; i < strlen(expression); i++)
	{
		// if(expression[i+1] = )
	}
}